name: qris_sticker
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_gallery_saver_plus: ^3.0.5
  get: ^4.6.6
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  screenshot: ^3.0.0
  sqflite: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
